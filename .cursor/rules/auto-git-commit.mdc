# Auto Git Commit Rule

## 规则描述
AI 在完成代码修改后自动进行 git 提交。

## 触发条件
当 AI 完成以下操作后，自动执行 git commit：
- 新增功能
- 修复 bug
- 重构代码
- 修改配置文件

## 执行规则

每次 AI 完成代码编辑任务后，必须执行：

1. 检查是否有改动：`git status`
2. 如果有改动，执行：
   ```bash
   git add -A
   git commit -m "<type>: <简短描述>"
   ```

## 提交信息格式

根据改动类型选择前缀：
- `feat:` 新功能
- `fix:` 修复 bug
- `refactor:` 重构
- `style:` 样式/格式调整
- `docs:` 文档更新
- `chore:` 配置/依赖更新

示例：
- `feat: 添加项目搜索功能`
- `fix: 修复列表渲染问题`
- `refactor: 提取公共组件`

## 例外情况

以下情况不自动提交：
- 用户明确说"先不要提交"
- 代码还在调试中，未完成
- 只是查看/阅读代码，没有修改
