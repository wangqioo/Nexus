# Nexus 项目导入规范

## 导入流程

每次导入新项目，按以下 5 步操作：

### 第 1 步：收集项目信息
扫描目标项目目录，收集：
- README.md 内容
- 配置文件类型（sdkconfig / CMakeLists.txt / package.json / platformio.ini / go.mod 等）
- 芯片/框架/语言/技术栈
- 功能描述

### 第 2 步：添加到 `~/.nexus/local-projects.json`
在 `projects` 数组末尾追加条目，格式根据 `projectType` 不同而变化：

---

#### MCU 项目
```json
{
  "id": "proj-<简短英文ID>",
  "name": "中文项目名",
  "path": "/Users/wq/Workshop/MCU/<分类>/<项目名>",
  "description": "一句话描述功能",
  "projectType": "mcu",
  "chip": "ESP32-S3",
  "framework": "ESP-IDF",
  "tags": ["芯片", "关键功能1", "关键功能2"],
  "hasSil": false,
  "documentCount": 0,
  "status": "active"
}
```
**必填字段**: id, name, path, description, projectType, chip, framework, tags, hasSil, status
**framework 常用值**: `ESP-IDF`, `Arduino`, `PlatformIO`, `MicroPython`, `RT-Thread`, `CanMV MicroPython`

---

#### AI 项目
```json
{
  "id": "proj-<ID>",
  "name": "中文项目名",
  "path": "/Users/wq/Workshop/AI/<项目名>",
  "description": "一句话描述",
  "projectType": "ai",
  "modelType": "CNN/LLM/Diffusion/...",
  "aiFramework": "PyTorch/TensorFlow/RKNN-Lite2/Anthropic API/...",
  "tags": ["AI", "关键词"],
  "hasSil": false,
  "status": "active"
}
```
**特有字段**: `modelType`(可选), `aiFramework`

---

#### 软件/Web 项目
```json
{
  "id": "proj-<ID>",
  "name": "中文项目名",
  "path": "/Users/wq/Workshop/Software/<语言分类>/<项目名>",
  "description": "一句话描述",
  "projectType": "software",
  "techStack": ["Go", "React", "SQLite"],
  "tags": ["语言", "关键词"],
  "hasSil": false,
  "status": "active"
}
```
**特有字段**: `techStack`(数组), `database`(可选)
**SDK/工具库 id 前缀用 `sdk-`**

---

#### Linux 平台项目
```json
{
  "id": "proj-<ID>",
  "name": "中文项目名",
  "path": "/Users/wq/Workshop/Linux/<项目名>",
  "description": "一句话描述",
  "projectType": "linux",
  "distro": "Debian/Ubuntu/Buildroot",
  "kernel": "Linux 5.10",
  "tags": ["Linux", "芯片", "关键词"],
  "hasSil": false,
  "status": "active"
}
```
**特有字段**: `distro`, `kernel`(可选)

---

#### 移动端项目
```json
{
  "id": "proj-<ID>",
  "name": "中文项目名",
  "path": "/Users/wq/Workshop/Mobile/<平台>/<项目名>",
  "description": "一句话描述",
  "projectType": "mobile",
  "mobilePlatform": "iOS/Android/watchOS/macOS/Flutter",
  "tags": ["平台", "关键词"],
  "hasSil": false,
  "status": "active"
}
```
**特有字段**: `mobilePlatform`

---

#### 远程设备
```json
{
  "id": "proj-<ID>",
  "name": "中文项目名",
  "path": "/Users/wq/Workshop/Remote/<设备名>",
  "description": "一句话描述",
  "projectType": "remote",
  "host": "设备名称或IP",
  "tags": ["设备", "关键词"],
  "hasSil": false,
  "status": "active"
}
```
**特有字段**: `host`

---

### 第 3 步：更新文档 `docs/wq-projects-overview.md`
在对应章节末尾添加条目，格式：
```markdown
#### N. 项目名 🆕
- **路径**: `相对路径`
- **框架**: XXX | **芯片**: XXX
- **描述**: 一句话描述
- **状态**: ✅ 活跃
```
同时更新文件顶部的统计数字。

---

### 第 4 步：创建知识库条目
存放在 `~/.nexus/knowledge/<projectType>/<category>/` 下。

#### 知识分类体系

| 项目类型 | 可用分类 |
|---------|---------|
| mcu | `platforms/`, `peripherals/`, `configs/`, `debug/`, `snippet/` |
| ai | `model/`, `training/`, `inference/`, `dataset/`, `prompt/` |
| software | `architecture/`, `api/`, `database/`, `deployment/`, `debug/` |
| linux | `system/`, `driver/`, `network/`, `cross-compile/`, `debug/` |
| mobile | `ui/`, `native/`, `network/`, `performance/`, `debug/` |
| remote | `connection/`, `deployment/`, `monitoring/`, `debug/` |

#### 知识条目格式

**JSON 格式** (平台/外设/配置类):
```json
{
  "id": "简短唯一ID",
  "name": "条目名称",
  "description": "简要描述",
  "platformId": "关联平台ID (可选)",
  "peripheralIds": ["关联外设ID"],
  "files": [
    {
      "filename": "文件名",
      "description": "说明",
      "content": "Markdown 内容"
    }
  ],
  "sourceProject": "来源项目名",
  "tags": ["标签1", "标签2"],
  "createdAt": "2026-02-14T12:00:00.000Z",
  "updatedAt": "2026-02-14T12:00:00.000Z"
}
```

**Markdown 格式** (调试经验/代码片段/笔记):
```markdown
---
title: 条目标题
tags: [标签1, 标签2, 标签3]
created: 2026-02-14
sourceProject: 来源项目名
---

# 标题

## 内容
正文用 Markdown 书写...
```

#### 什么时候创建知识条目
- **新芯片/框架组合** → `platforms/` JSON
- **新外设/传感器** → `peripherals/` JSON
- **有价值的配置** → `configs/` JSON
- **可复用的代码** → `snippet/` Markdown
- **调试踩坑经验** → `debug/` Markdown
- **架构设计方案** → `architecture/` JSON
- **模型部署经验** → `inference/` 或 `model/` JSON

---

### 第 5 步：创建项目笔记
存放在 `~/.nexus/notes/` 下，为每个有价值的项目创建开发笔记。

#### 笔记格式

**JSON 格式** (结构化技术笔记):
```json
{
  "id": "项目名-主题",
  "title": "中文标题",
  "content": "# 标题\n\nMarkdown正文（用\\n换行）",
  "platformId": "关联平台ID (可选)",
  "peripheralIds": ["关联外设ID"],
  "tags": ["标签1", "标签2"],
  "createdAt": "2026-02-14T12:00:00.000Z",
  "updatedAt": "2026-02-14T12:00:00.000Z"
}
```

**Markdown 格式** (自由笔记):
```markdown
---
title: 笔记标题
tags: [标签1, 标签2, 标签3]
created: 2026-02-14
---

# 标题

正文用 Markdown 书写...
```

#### 笔记应包含的内容
- **项目概要**: 做什么、为什么做
- **系统架构**: 软硬件架构图 (ASCII 即可)
- **技术要点**: 核心算法、关键设计决策
- **硬件组成**: 芯片、传感器、外设清单
- **子项目索引**: 如果是项目集，列出所有子项目
- **经验总结**: 踩坑、选型建议、性能数据
- **项目关联**: 与其他项目的关系

#### 什么项目需要笔记
- 功能复杂的项目 (多子项目、多模块)
- 有独特技术方案的项目
- 涉及新芯片/新框架的项目
- 多个项目有关联关系时 (如 FOC 系列)
- 项目间有对比价值时 (如三块 ESP32-P4 开发板)

#### 命名规范
- 文件名: `<项目名>-<主题>.json` 或 `.md`
- 例: `foc-projects-overview.md`, `esp32p4-development-boards.json`

---

## 目录结构规范

新项目应放在对应分类目录下：
```
/Users/wq/
├── Workshop/MCU/          # MCU/嵌入式
│   ├── ESP32-S3/
│   ├── ESP32-P4/
│   ├── ESP32-C5/
│   ├── ESP32-C6x/
│   ├── ESP32/
│   ├── Arduino/
│   ├── FOC-Projects/
│   ├── MicroPython/
│   ├── SiFli/
│   ├── K230/
│   ├── RK3588/
│   └── xiaozhi/
├── Workshop/AI/           # AI/ML
├── Workshop/Software/     # 软件/Web
│   ├── Go/
│   ├── Node/
│   ├── Python/
│   └── Web/
├── Workshop/Linux/        # Linux
├── Workshop/Mobile/       # 移动端
│   ├── iOS/
│   ├── Android/
│   ├── watchOS/
│   └── AR/
├── Workshop/Remote/        # 远程设备
├── SDK/                   # SDK和工具库
└── Nexus/           # 本项目
```

## ID 命名规范
- 项目 ID: `proj-<简短英文名>` (如 `proj-inktime`)
- SDK ID: `sdk-<库名>` (如 `sdk-simplefoc`)
- 知识条目 ID: `<描述性英文>` (如 `esp32s3-yolo-detect`)
- 避免中文、空格、特殊字符
